/**
 * Permissions demandées quand on invite le bot sur un serveur.
 * « Administrateur » couvre les besoins Vex (salons, rôles, messages, modération, etc.).
 */
export const BOT_INVITE_PERMISSIONS = 8n;
