import { AuthenticatedSection } from "../components/layout/AuthenticatedSection.js";

export function TicketsPage() {
  return (
    <AuthenticatedSection title="Tickets" description="Système de tickets.">
      Contenu à venir.
    </AuthenticatedSection>
  );
}
